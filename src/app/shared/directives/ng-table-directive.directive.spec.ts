import { NgTableDirectiveDirective } from './ng-table-directive.directive';

describe('NgTableDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NgTableDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
